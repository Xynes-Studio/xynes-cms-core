{
  "name": "xynes-cms-core",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun --env-file=.env.localhost run --watch src/index.ts",
    "start": "bun --env-file=.env.localhost run src/index.ts",
    "test": "bun --env-file=.env.localhost test",
    "db:generate": "drizzle-kit generate:pg",
    "db:push": "drizzle-kit push:pg",
    "db:seed": "bun --env-file=.env.localhost run src/infra/db/seed.ts",
    "test:coverage": "bun --env-file=.env.localhost test --coverage",
    "lint": "bun x biome check src"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "drizzle-orm": "^0.29.0",
    "hono": "^4.0.0",
    "postgres": "^3.4.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.20.0",
    "@types/pg": "^8.11.0"
  }
}